import { sum } from './helpers'


describe('Tests Cardio Day 1', () => {
  test('My 1st Set Of Test', () => {
  test('Sum up the instances of each of these', () => {
    expect(sum(1, 1)).toEqual(2)
  })
})
